defaults:
  - data: parallel_wmt24pp_flores
  - model: qwen3_4b_base
  - finetune: full
  - preprocessing: default
  - training: gpu96
  - evaluation: gpu96
  - logging: wandb
  - _self_

seed: 42

experiment:
  name: "parallel-cpt-full-96gb-unsloth-qwen3-4b-base"
  output_root: "artifacts/cpt_parallel_full_96gb_unsloth_qwen3_4b_base"
  processed_dir: "data/processed/cpt_parallel"

hydra:
  run:
    dir: ${experiment.output_root}/hydra/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ${experiment.output_root}/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
