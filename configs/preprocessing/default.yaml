output_dir: ${experiment.processed_dir}
overwrite_output: false

cleaning:
  min_chars: 0

chunking:
  paragraph_separator: "\n\n"

packing:
  enabled: false
  strategy: "bfd_split"
  max_length: ${model.max_seq_length}

split:
  val_ratio: 0.02
  seed: ${seed}
