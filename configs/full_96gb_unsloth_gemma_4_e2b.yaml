defaults:
  - data: parallel_wmt24pp_flores
  - model: gemma_4_e2b
  - finetune: full
  - preprocessing: default
  - training: gpu96
  - evaluation: gpu96
  - logging: wandb
  - _self_

seed: 42

experiment:
  name: "parallel-cpt-full-96gb-unsloth-gemma-4-e2b"
  output_root: "artifacts/cpt_parallel_full_96gb_unsloth_gemma_4_e2b"
  processed_dir: "data/processed/cpt_parallel"

hydra:
  run:
    dir: ${experiment.output_root}/hydra/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ${experiment.output_root}/multirun/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
